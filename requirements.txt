# ============================================================
# ML + Quants Trading Agent — Dependencies
# ============================================================
# Phase 1: Core libraries pinned to latest stable as of Sep 2026.
# Add phase-specific deps below the relevant section header.
# ============================================================

# --- Data & Numerical Computing ---
pandas>=2.2,<3
numpy>=1.26,<2
statsmodels>=0.14,<1

# --- Market Data ---
yfinance>=0.2,<1

# --- ML / Boosting ---
scikit-learn>=1.5,<2
xgboost>=2.1,<3
lightgbm>=4.4,<5

# --- Deep Learning ---
torch>=2.3,<3
transformers>=4.42,<5

# --- NLP (shared with transformers) ---
# transformers already listed above

# --- Hyperparameter Optimisation ---
optuna>=3.6,<4

# --- Backtesting ---
vectorbt>=0.26,<1

# --- Execution / Broker API ---
alpaca-py>=0.30,<1

# --- Volatility Modelling (GARCH) ---
arch>=7.0,<8

# --- Explainability ---
shap>=0.45,<1

# --- Visualisation ---
matplotlib>=3.9,<4
plotly>=5.22,<6

# --- Dashboard ---
streamlit>=1.36,<2

# --- Config & Utilities ---
pyyaml>=6.0,<7
python-dotenv>=1.0,<2

# --- Dev / Testing ---
pytest>=8.2,<9
pytest-cov>=5.0,<6
ruff>=0.5,<1
